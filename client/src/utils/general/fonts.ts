import { Varela_Round, Roboto } from 'next/font/google';

export const varelaRound = Varela_Round({ weight: '400', subsets: ['latin'], display: 'swap', variable: '--fontVarelaRound' });

export const roboto = Roboto({ weight: '400', subsets: ['latin'], display: 'swap', variable: '--fontRoboto' });

